[project]
name = "mimic-preprocessor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.3.1",
    "pyarrow>=20.0.0",
    "tomli>=2.2.1",
]
